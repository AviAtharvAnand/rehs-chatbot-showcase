apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: chroma-data
  namespace: rehs-2026-chatbot
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: rook-ceph-block